import React from 'react'
import ReactDOM from 'react-dom'
import API from './App'

ReactDOM.render(
    <React.StrictMode>
        <API />
    </React.StrictMode>,
    document.getElementById('root')
)